<link rel="stylesheet" href="/notInjectedStyles/general.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.7.2/Chart.min.js"></script>
<!-- <div class="alert alert-warning" role="alert">
    Under construction
</div> -->
<div class="container" style="text-align: center">
    <div class="container" style="margin-top: 10px;">
        <h4>Top 6 most reported FAQS</h4>
        <canvas id="myChart" width="600" height="300" style="margin-bottom: 40px"></canvas>
        <script>
        var ctx = document.getElementById("myChart").getContext('2d');
        var faqs =  <%- faqsWithReports %>;
        //console.log(faqs);
        var myChart = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: faqs.map(f => f.question.slice(0, 24) + "..."),
                datasets: [{
                    label: 'number of reports',
                    data: faqs.map(f => f.reports),
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                        'rgba(255, 206, 86, 0.2)',
                        'rgba(75, 192, 192, 0.2)',
                        'rgba(153, 102, 255, 0.2)',
                        'rgba(255, 159, 64, 0.2)'
                    ],
                    borderColor: [
                        'rgba(255,99,132,1)',
                        'rgba(54, 162, 235, 1)',
                        'rgba(255, 206, 86, 1)',
                        'rgba(75, 192, 192, 1)',
                        'rgba(153, 102, 255, 1)',
                        'rgba(255, 159, 64, 1)'
                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero:true
                        }
                    }],
                    xAxes: [
                        {
                            ticks: {
                                autoSkip: false,
                                stepSize: 1
                            }
                        }
                    ]
                },

                onClick: function(ev, arr){
                    let index = arr[0]._index;
                    let makeLink = "/allFaqs?id=" + faqs[index].id;
                    location.href = makeLink;
                }
            }
        });
        </script>
    </div>
</div>